# Projeto

## Instalação

`composer install`

`npm install`

## Configuração

`.env.default` > `.env`

`php artisan migrate`

`php artisan db::seed`

## Rodando

`php artisan serve`

`npm run watch`


## Core
* PHP 7
* Laravel 5.6
* Admimbsb
